services:
  - type: web
    name: new-high-screener
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_server:app --bind 0.0.0.0:$PORT --timeout 600 --workers 2 --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
